repertoire principal contenant le travail de mon stage:

	-document stage: contient mes presentations, et rapports, plus les articles utiles.

	-fichier_utiles: contient essentiellement les fichiers annotés avec les deriving_show, puis des fichiers .mli, .cmi dépendant de ces fichiers et un script faisant le lien.

	-makefile : contient les Makefile de mon stage que j'ai eu besoin de modifié.

	-dvpmt_ocaml : contient le code que j'ai developpé en caml une fois les données de la representation RTL sont récupérés.

	-resultats : contient des resultats RTL, et des resultats au format Z3.

	
